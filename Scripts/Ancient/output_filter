sed - stream editor for filtering and transforming text

simple sed command to extract the hashed passwords (everything after the ':') into a file named hashes.txt:


sed -n 's/.*://p' formated_hash.txt > hashes.txt

=====================================================

awk -F'@' '{print $1}' formated_hash.txt > users.txt

command to output the words before @ from each line
