```
#To create a new Python virtual environment in the .venv directory.
python3 -m venv .venv

#To activate a Python virtual environment located in the .venv directory.
source .venv/bin/activate

python3 -m pip install -r requirements.txt
```
